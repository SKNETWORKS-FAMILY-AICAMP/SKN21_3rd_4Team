# 📚 Bootcamp AI Tutor - 전체 프로젝트 흐름 설명

## 🎯 프로젝트 개요

RAG(Retrieval-Augmented Generation) 기반 학습 지원 챗봇으로, 부트캠프 학습 자료와 Python 공식 문서를 활용하여 개념·코드·실습 질문에 답변합니다.

---

## 🔄 전체 흐름도

```
사용자 질문 입력
    ↓
[Frontend: index.html]
    ↓ POST /chat/stream
[Flask Backend: app.py]
    ↓ learning_agent()
[main.py: main()]
    ↓ LangGraph Workflow
[workflow.py: build_graph()]
    ↓
┌─────────────────────────────────────┐
│  START                              │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│  1. search_node                     │
│     - search_router: 질문 분석      │
│     - search_agent: 하이브리드 검색 │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│  2. build_context                   │
│     - 검색 결과 포맷팅               │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│  3. check_relevance                 │
│     - 평균 유사도 점수 계산          │
│     - 분기 결정                      │
└──────────────┬──────────────────────┘
               ↓
      ┌────────┴────────┐
      │                 │                 │
      ↓                 ↓                 ↓
┌──────────┐   ┌──────────┐      ┌──────────┐
│ no_data  │   │ web_     │      │ analyst  │
│ _node    │   │ search_  │      │ _node    │
│          │   │ node     │      │          │
│ (≤0.3)   │   │ (0.3~0.5)│      │ (>0.5)   │
└────┬─────┘   └────┬─────┘      └────┬─────┘
     │              │                 │
     │              └────────┬────────┘
     │                       ↓
     │              ┌─────────────────┐
     │              │  analyst_node   │
     │              └─────────────────┘
     │                       │
     └───────────┬───────────┘
                 ↓
            ┌────────┐
            │  END   │
            └────────┘
                 ↓
      [Flask: 응답 반환]
                 ↓
      [Frontend: 스트리밍 표시]
```

---

## 📝 단계별 상세 설명

### 🎨 1단계: Frontend (사용자 인터페이스)

**파일**: `templates/index.html`

- 사용자가 질문을 입력하면 `POST /chat/stream` 엔드포인트로 요청
- Server-Sent Events (SSE)를 통한 실시간 스트리밍 응답
- 진행 단계, 답변 텍스트, 추천 질문, 참고 자료를 순차적으로 표시

```javascript
// 주요 동작
const response = await fetch('/chat/stream', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message: msg, mode: currentMode })
});
```

---

### 🌐 2단계: Flask Backend (API 서버)

**파일**: `app.py`

#### 주요 엔드포인트

1. **`POST /chat/stream`** (스트리밍 채팅)
   - 사용자 질문을 받아 `learning_agent()` 호출
   - 응답을 SSE 형식으로 스트리밍

2. **`GET /health`** (헬스 체크)
   - 서버 및 Qdrant DB 연결 상태 확인

3. **`GET /api/quiz`** (퀴즈 데이터)
   - Qdrant에서 퀴즈 데이터 조회

#### `learning_agent()` 함수

```python
def learning_agent(message, context=None):
    # main.py의 main() 함수 호출
    response = main(message)
    
    # LangGraph 응답에서 데이터 추출
    - analyst_results: AI 답변
    - search_results: 검색된 문서들
    - suggested_questions: 추천 질문들
    
    # 마크다운 형식으로 포맷팅하여 반환
    return {
        'text': answer_text,  # 포맷팅된 답변
        'sources': sources,   # 참고 자료 카드
        'suggested_questions': suggested_questions,
        'steps': [...]  # 진행 단계 정보
    }
```

---

### ⚙️ 3단계: LangGraph Workflow 실행

**파일**: `main.py`

```python
def main(query: str):
    # LangGraph 워크플로우 생성
    graph = build_graph()
    graph = graph.compile(checkpointer=MemorySaver())
    
    # 초기 상태 설정
    agent_state = AgentState(
        query=query,
        messages=[("human", query)],
    )
    
    # 워크플로우 실행 (invoke)
    response = graph.invoke(agent_state, config=config)
    
    return response
```

**상태 관리** (`src/schema/state.py`):
- `query`: 사용자 질문
- `messages`: 대화 기록 (자동 누적)
- `search_results`: 검색 결과 리스트
- `context`: 포맷팅된 컨텍스트 텍스트
- `analyst_results`: 최종 AI 답변
- `suggested_questions`: 추천 질문 리스트

---

### 🔍 4단계: Search Node (검색 실행)

**파일**: `src/agent/nodes/search_node.py`

#### 4-1. Search Router (`search_router.py`)

**역할**: 질문을 분석하여 검색 설정 결정

**프로세스**:
1. LLM(GPT-4o-mini)이 질문 분석
2. Structured Output (Pydantic)으로 검색 설정 생성

**결정 사항**:
- `sources`: 검색할 소스 (`['lecture']`, `['python_doc']`, 또는 둘 다)
- `top_k`: 검색할 문서 개수 (기본: 3, 중급: 5, 고급: 7)
- `search_method`: 검색 방법 (`similarity` 또는 `mmr`)
- `is_valid`: 질문 유효성 검증

```python
class SearchConfig(BaseModel):
    is_valid: bool  # 질문이 학습 자료 관련인지
    query_type: Literal['concept', 'code', 'syntax']
    complexity: Literal['basic', 'intermediate', 'advanced']
    search_sources: List[Literal['lecture', 'python_doc']]
    top_k: int
    search_method: Literal['similarity', 'mmr']
```

#### 4-2. Search Agent (`search_agent.py`)

**역할**: 실제 하이브리드 검색 실행

**하이브리드 검색 구조**:
1. **Vector Search** (의미 유사도)
   - 임베딩 벡터 간 코사인 유사도
   - 후보를 넉넉히 확보 (`candidate_k = top_k * 4~6`)

2. **Keyword Matching** (정확 용어 매칭)
   - 질문 키워드가 문서에 포함되어 있는지 확인
   - 매칭 비율로 점수 계산 (0.0 ~ 1.0)

3. **BM25** (Best Matching 25)
   - TF(단어 빈도) + IDF(역문서 빈도) + 문서 길이 정규화

4. **가중합 계산**
   - 일반 쿼리: Vector 0.6 / Keyword 0.2 / BM25 0.2
   - 단일 단어: Vector 0.4 / Keyword 0.3 / BM25 0.3

5. **듀얼 쿼리** (Python 문서 최적화)
   - 한글 질문 → 영어 키워드로 번역 후 검색
   - 번역 결과가 약하면 원문으로 fallback

**검색 결과 형식**:
```python
{
    "content": "...",  # 문서 내용
    "score": 0.8523,   # 최종 하이브리드 점수
    "metadata": {
        "source": "lecture" or "python_doc",
        "lecture_title": "...",
        ...
    }
}
```

---

### 📋 5단계: Build Context (컨텍스트 포맷팅)

**파일**: `src/agent/nodes/search_node.py` → `build_context()`

**역할**: 검색 결과를 LLM이 읽기 쉬운 형식으로 포맷팅

```python
# 각 검색 결과를 다음과 같이 포맷팅:
"[Original 1] 출처: lecture (유사도: 0.85)
=== [내부 자료 (Original)] === 

[문서 내용...]

---

[Original 2] 출처: python_doc (유사도: 0.72)
..."
```

---

### 🎯 6단계: Check Relevance (관련성 평가)

**파일**: `src/agent/nodes/analyst_node.py` → `check_relevance()`

**역할**: 검색 결과의 평균 유사도 점수 계산 후 분기 결정

**분기 로직**:
```python
avg_score = sum(scores) / len(scores)

if avg_score <= 0.3:
    return "no_data_node"      # 관련 자료 없음
elif avg_score <= 0.5:
    return "web_search_node"   # 외부 웹 검색 추가
else:
    return "analyst_node"      # 내부 자료만으로 충분
```

---

### 🤖 7단계: Analyst Node (최종 답변 생성)

**파일**: `src/agent/nodes/analyst_node.py`

**역할**: 검색된 컨텍스트를 바탕으로 최종 답변 생성

**프로세스**:
1. System Prompt + Context + Query를 LLM에 전달
2. Structured Tool Calling으로 구조화된 답변 생성
3. 답변 형식:
   - `summary`: 요약
   - `code_explanation`: 코드 설명
   - `practice_tips`: 실습 팁
   - `one_liner`: 한 줄 정리
   - `suggested_questions`: 추천 질문

```python
# Tool 사용
submit_analysis(
    summary="...",
    code_explanation="...",
    practice_tips="...",
    one_liner="...",
    suggested_questions=["질문1", "질문2", "질문3"]
)
```

---

### 🌐 7-1. Web Search Node (Fallback)

**파일**: `src/agent/nodes/analyst_node.py` → `web_search_node()`

**역할**: 내부 문서 점수가 낮을 때 외부 웹 검색 추가

**프로세스**:
1. Tavily Search API 호출
2. 외부 검색 결과를 context에 추가
3. 이후 `analyst_node`로 전달하여 답변 생성

**조건**: 평균 유사도가 0.3 ~ 0.5일 때 실행

---

### ❌ 7-2. No Data Node (관련 자료 없음)

**파일**: `src/agent/nodes/analyst_node.py` → `no_data_node()`

**역할**: 관련 자료가 없을 때 고정 메시지 반환

**프로세스**:
1. GPT를 호출하지 않음 (비용 절약)
2. 고정 메시지 반환
3. 랜덤 추천 질문 3개 제공

**조건**: 평균 유사도가 0.3 이하일 때 실행

---

### 📤 8단계: 응답 반환 및 표시

**Flask에서 최종 응답 형식**:
```python
{
    'text': "...",  # 마크다운 형식의 답변
    'sources': [    # 참고 자료 카드 (상위 3개)
        {
            'type': 'LECTURE',
            'title': '강의 제목',
            'content': '내용 미리보기...',
            'score': 0.85
        },
        ...
    ],
    'suggested_questions': ["질문1", "질문2", "질문3"],
    'steps': [
        {'step': 1, 'title': 'Router', 'desc': '...'},
        {'step': 2, 'title': 'Search', 'desc': '...'},
        {'step': 3, 'title': 'Analyst', 'desc': '...'}
    ]
}
```

**Frontend에서 표시**:
1. 진행 단계 표시 (Router → Search → Analyst)
2. 답변 텍스트 스트리밍 (글자 하나씩)
3. 추천 질문 버튼 표시
4. 참고 자료 카드 표시

---

## 🔑 핵심 기술 스택

### Backend
- **Flask**: 웹 서버 프레임워크
- **LangGraph**: 워크플로우 관리
- **LangChain**: LLM 통합 및 Chain 관리
- **OpenAI GPT-4o-mini**: 질문 분석 및 답변 생성

### Vector Database
- **Qdrant**: 벡터 검색 엔진
- **OpenAI Embeddings**: 텍스트 임베딩 (text-embedding-3-large)

### Search
- **하이브리드 검색**: Vector + Keyword + BM25
- **듀얼 쿼리**: 한글 질문 → 영어 번역 검색
- **Fallback**: Tavily 웹 검색

### Frontend
- **HTML/CSS/JavaScript**: 클라이언트 UI
- **Server-Sent Events (SSE)**: 실시간 스트리밍
- **Marked.js**: 마크다운 렌더링
- **Highlight.js**: 코드 하이라이팅

---

## 💡 주요 특징

1. **하이브리드 검색**: 의미 유사도 + 키워드 매칭 + BM25를 결합하여 검색 품질 향상
2. **스마트 라우팅**: 질문 난이도에 따라 검색 개수 자동 조정
3. **Fallback 메커니즘**: 내부 자료가 부족하면 외부 웹 검색 자동 추가
4. **비용 최적화**: 관련 자료가 없을 때는 GPT 호출하지 않음
5. **사용자 경험**: 실시간 스트리밍, 진행 단계 표시, 추천 질문 제공

---

## 📊 데이터 흐름 요약

```
사용자 질문
  ↓
[질문 분석] → 검색 설정 결정 (Router)
  ↓
[하이브리드 검색] → Qdrant에서 문서 검색
  ↓
[컨텍스트 포맷팅] → LLM이 읽기 쉬운 형식으로 변환
  ↓
[관련성 평가] → 평균 점수 계산
  ↓
[분기]
  ├─ 점수 높음 → Analyst Node (내부 자료만 사용)
  ├─ 점수 중간 → Web Search + Analyst Node (외부 추가)
  └─ 점수 낮음 → No Data Node (고정 메시지)
  ↓
[최종 답변 생성] → 구조화된 형식 (요약/코드/팁/정리)
  ↓
[응답 반환] → Frontend로 스트리밍 전송
```

---

이제 프로젝트의 전체 흐름을 이해하셨나요? 더 자세히 알고 싶은 부분이 있으면 알려주세요! 🚀